global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: naulite
    file_sd_configs:
      - files:
          - /etc/prometheus/file_sd/*.json
        refresh_interval: 30s
