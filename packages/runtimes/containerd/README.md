# @platform/runtime-containerd

containerd `RuntimeProvider` stub that returns a not-configured error until containerd is wired on the node.

See [platform context](../../CONTEXT.md) for architecture and integration details.
