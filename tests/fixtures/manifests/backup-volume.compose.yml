name: backup-volume

volumes:
    app-data:
        backup:
            schedule: "* * * * *"
            destination:
                provider: local
                path: /var/lib/platform/backups

services:
    app:
        image: nginx:1.27-alpine
        volumes:
            - app-data:/data
        restart: unless-stopped
