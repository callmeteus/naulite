name: CI

on:
    push:
        branches: [main, master]
    pull_request:

env:
    # Keep in sync with infra/zig-toolchain.env and packages/agent/Dockerfile ARG ZIG_VERSION
    ZIG_VERSION: "0.16.0"
    REQUIRE_DOCKER: "true"

jobs:
    verify:
        runs-on: ubuntu-latest
        timeout-minutes: 60

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: "22"
                  cache: yarn
                  cache-dependency-path: yarn.lock

            - name: Setup Zig
              uses: mlugg/setup-zig@v2
              with:
                  version: ${{ env.ZIG_VERSION }}

            - name: Build Zig agent
              working-directory: packages/agent
              run: zig build -Doptimize=ReleaseSafe

            - name: Test Zig agent
              working-directory: packages/agent
              run: zig build test

            - name: Build Zig CLI
              working-directory: packages/cli
              run: zig build

            - name: Install dependencies
              run: yarn install --frozen-lockfile

            - name: Lint
              run: yarn lint

            - name: Unit tests
              run: yarn test:unit

            - name: Verify Docker
              run: |
                  docker version
                  docker info

            - name: Docker unit tests
              run: yarn test:unit:docker

            - name: Build
              run: yarn build

    docker-smoke:
        runs-on: ubuntu-latest
        timeout-minutes: 30

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Build agent image
              run: docker build -t platform-agent -f packages/agent/Dockerfile .

            - name: Build control-plane image
              run: docker build -t platform-control-plane -f packages/control-plane/Dockerfile .

            - name: Build UI image
              run: docker build -t platform-ui -f packages/ui/Dockerfile .

            - name: Build UI backend image
              run: docker build -t platform-ui-backend -f packages/ui/packages/backend/Dockerfile .

    image-scan:
        needs: docker-smoke
        runs-on: ubuntu-latest
        timeout-minutes: 45

        strategy:
            fail-fast: false
            matrix:
                include:
                    - image: platform-agent
                      dockerfile: packages/agent/Dockerfile
                    - image: platform-control-plane
                      dockerfile: packages/control-plane/Dockerfile
                    - image: platform-ui
                      dockerfile: packages/ui/Dockerfile
                    - image: platform-ui-backend
                      dockerfile: packages/ui/packages/backend/Dockerfile

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Build image
              run: docker build -t ${{ matrix.image }} -f ${{ matrix.dockerfile }} .

            - name: Run Trivy vulnerability scanner
              uses: aquasecurity/trivy-action@v0.36.0
              with:
                  image-ref: ${{ matrix.image }}
                  format: table
                  exit-code: "1"
                  ignore-unfixed: true
                  vuln-type: os,library
                  severity: CRITICAL
